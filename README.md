# Portfólio

Instale as dependências com `npm install` e inicie com `npm run dev`.

Para habilitar o contato, crie `.env` a partir de `.env.example` e informe as três variáveis do EmailJS. O template recebe `subject` e `message`.
